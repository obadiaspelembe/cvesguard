---
version: v1.0.0 
spec:
  config:
    vulnerability:
      critical: 1
      high: 40
      medium: 12
      low: 10
    packages:
      - name: stdlib
        action: deny
        severity:
          - critical
          - high

