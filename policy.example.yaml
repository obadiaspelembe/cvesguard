---
version: v1.0.0 
spec:
  config:
    vulnerability:
      critical: 8
      high: 123
      medium: 2
      low: 2
    packages:
      - name: log4j
        action: ignore
        severity:
          - critical
          - high

